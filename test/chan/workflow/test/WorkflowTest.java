package chan.workflow.test;



import org.junit.Assert;
import org.junit.Test;

import chan.workflow.FlowNotFoundException;
import chan.workflow.Workflow;
import chan.workflow.WorkflowImpl;

public class WorkflowTest {

	@Test
	public void testFlow() {
		/*Workflow w = new WorkflowImpl();
		
		String flow = "<flow name=\"approve\"><activity id=\"s1\" class=\"chan.retailer.workflow.SimpleActivity\">"
				+ "<input><id /><name /></input>"
				+ "<output><oId /><oName /></output>"
				+ "</activity></flow>";
		
		w.load(flow);
		try {
			w.start();
		} catch (FlowNotFoundException e) {
			Assert.fail(e.getMessage());
		}		
		Assert.assertTrue(true);*/
	}
}
